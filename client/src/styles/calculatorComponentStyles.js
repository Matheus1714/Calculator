import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles({
    root: {
      minWidth: 275,
      maxWidth: 350,
      minHeight: 400,
      flexGrow: 1,
      alignItems: 'center',
      textAlign: 'center'
    },
})
